# AGENTS.md — 财务拆分作业说明书（模板）

> 本文件是给 AI Agent 读的执行规范。**任何 Agent 在本项目工作时必须遵守本文件。**
> 使用方法：复制本文件到公司工作目录，把 `<尖括号>` 占位换成该公司实际情况，再追加「公司特化」段落。

## 0. 任务

把 `<公司名>` 的财务数据拆分到标准化成品表：按 **行业 / 产品 / 地区 / 销量 / 客户 / 供应商 / 费用** 各板块，按报告期（年报 12/31、半年报 6/30）铺开，并保持跨年口径可比。

## 1. 目录约定

```
<公司>/
├── AGENTS.md            本说明书（含公司特化段落）
├── statutory_filings/   定期报告原文：年报 / 半年报 / 招股说明书（按期间命名）
├── financial_results/   业绩快报 / 决算说明等补充文件（如适用）
├── data/                中间表（每个板块一张或多张）
├── outputs/             成品表
├── tmp_pdf_text/        PDF 转出的 txt（可重建，不入库）
└── scripts/             本项目专用提取与灌表脚本
```

原始文件只读不改；任何修复走脚本，不手工改成品。

## 2. 数据来源优先级

1. 定期报告原文（年报 / 半年报 / 招股说明书）——**一手，最高优先级**
2. 公司业绩快报 / 决算说明（如需精确到季度）
3. 第三方数据终端——仅用于披露缺失期间，且必须标注来源
4. 推算值——仅在两份一手数据之间做勾稽推导时使用，必须写明公式

**招股说明书覆盖的早期年份必须补入**，不能从第一份年报开始。申报稿与上市稿报告期可能不同，两版都要查。

## 3. 字段与结构规则

1. 每个项目按 **名称 / yoy / 毛利率 / 占比** 四行一组；板块之间空 2 行。
2. 日期列每年 4 个季度位（3/31、6/30、9/30、12/31）；无披露的季度留空占位，不删列。
3. `yoy` 只在**同报告期、同口径**之间计算（年报对年报、半年报对半年报）；口径变化或首次出现时留空，不写 0。
4. `占比 = 该项目营收 ÷ 营业收入合计 × 100%`，分母用利润表口径，不用倒减。
5. 「其他」的处理按板块区分：**行业板块排除**（不是真实行业）；**产品板块保留**（残差法，产品合计 = 总收入）；**地区板块**内外销应自洽。
6. 父类目与子项同时出现时（「其中：」结构），子项计入明细但不参与加总，避免重复计数。
7. 项目命名**不简写**，跨年改名链保留原报告各期名称并在备注说明。

## 4. 空值规则

- 披露未披露 → **留空**，并在交付说明中逐块给出原因（如「该年半年报未按行业披露」）
- 成品表 Sheet 1 空值一律 `None`，**禁止** `--`、`—`、`N/A` 等占位符
- 中间表可用 `--` 标记「已查证未披露」
- 任何情况下不得用其他期间/其他项目的数值顶替

## 5. 输出格式

| 类型 | 格式 |
|---|---|
| 日期 | `YYYY-MM-DD`，Arial 11，加粗 |
| 金额（亿） | `0.00` |
| yoy / 毛利率 / 占比 | `0.00%`，**单元格存小数**（0.1234 显示 12.34%） |
| 销量 | `#,##0.00` |
| 项目名 / 板块标题 | 宋体 11；项目名加粗 |

冻结窗格 B2。

## 6. 红线

1. **不覆盖已填内容**：目标格已有值时先判断，手工改过的值不得重写。
2. **不硬编码数据**：成品表只从中间表读值灌入；脚本里出现的数字只能是格式与行号。
3. **不编造**：核实不了的数据标「未披露」，绝不用估算值冒充实测值。
4. **不擅自改模板结构**：字段名、行序、板块顺序按模板原样，缺槽位先问再加。
5. **不做全量重建**：用户手工调整过布局的成品表，之后只允许定点写单元格，禁止用模板重建覆盖。
6. **单位统一为亿元**（万元 ÷ 1e4，元 ÷ 1e8），写入前逐表确认原始单位。

## 7. 交付前自检（必须全部通过）

- [ ] 行业加总 ≈ 总收入；产品加总 ≈ 总收入；地区加总 ≈ 总收入（差额需能解释为「其他业务」）
- [ ] 每个有值的金额格对应存在 yoy（同报告期有基期时）
- [ ] 日期列每年 4 个季度位齐全，3/31 与 9/30 无数据时为空
- [ ] 全表无 `--`/`—` 占位符（Sheet 1）
- [ ] 无 General 格式的数值格（金额、百分比、销量格式已设）
- [ ] 已知空值清单已随交付说明给出

## 8. 公司特化（换公司时在此追加）

- 行业披露模式：<按国民经济行业分类 / 单一行业 / 按客户类型>
- 产品线跨年命名演变：<旧名 → 新名，起止年份>
- 特殊数据来源：<该公司的申报稿、业绩快报、币种、对照版本要求等>
